<div class="box box-primary">
    <div class="box-body">
      <?= $this->flashMessenger()->render('success', array('alert', 'alert-dismissible', 'alert-success')); ?>
      <?= $this->flashMessenger()->render('error', array('alert', 'alert-dismissible', 'alert-error')); ?>
      <table id="example2" class="table table-bordered table-hover">
        <thead>
        <tr>
          <th>#</th>
          <th>Título</th>
          <th colspan="2">Ações</th>
        </tr>
        </thead>
        <tbody>
            <?php foreach ($this->lista as $entity): ?>
                <tr>
                  <td><?php echo $entity->getId(); ?></td>
                  <td><?php echo $entity->getTitulo(); ?></td>
                  <td>
                      <a href="<?= $this->url('categorialivro', array('action' => 'editar' , 'id' => $entity->getId())) ?>" class="btn btn-xs btn-default">
                        <i class="fa fa-edit"></i> <span class="hidden-xs">Editar</span>
                      </a>
                  </td>
                  <td>
                      <a href="<?= $this->url('categorialivro', array('action' => 'excluir' , 'id' => $entity->getId())) ?>" class="btn btn-xs btn-danger">
                        <i class="fa fa-trash-o"></i> <span class="hidden-xs">Excluir</span>
                      </a>
                  </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
        <tfoot>
        <tr>
            <th colspan="4">
              <a href="<?= $this->url('categorialivro', array('action' => 'cadastrar')) ?>" class="btn btn-primary"><i class="fa fa-plus-square-o"></i> Nova categoria</a>
            </th>
        </tr>
        </tfoot>
      </table>
    </div>
    <!-- /.box-body -->
</div>
<!-- /.box -->